spring.application.name=bella-spring-boot-sample
server.port=${PORT:8080}

# Spring datasource — uses ${DATABASE_URL} injected by BellaEnvironmentPostProcessor
# spring.datasource.url=${DATABASE_URL}
# spring.datasource.username=${DB_USER}
# spring.datasource.password=${DB_PASSWORD}

# Bella configuration (alternative to env vars)
# bella.url=https://baxter.yourcompany.com
# bella.api-key=bella_ak_xxxxx
# bella.secret-key=sk_xxxxx
# bella.env=production
